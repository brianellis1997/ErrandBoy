version: '3.8'

services:
  postgres:
    image: pgvector/pgvector:pg15
    container_name: groupchat-postgres
    environment:
      POSTGRES_USER: groupchat_user
      POSTGRES_PASSWORD: groupchat_pass
      POSTGRES_DB: groupchat
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/init_db.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U groupchat_user -d groupchat"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: groupchat-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Uncomment to run the app in Docker
  # app:
  #   build: .
  #   container_name: groupchat-app
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #   environment:
  #     DATABASE_URL: postgresql+asyncpg://groupchat_user:groupchat_pass@postgres:5432/groupchat
  #     REDIS_URL: redis://redis:6379/0
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - ./:/app
  #   command: poetry run uvicorn groupchat.main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  postgres_data:
  redis_data: